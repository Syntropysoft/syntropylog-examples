{
  "name": "@syntropylog/example-50-basic-kafka-correlation",
  "version": "1.0.0",
  "description": "SyntropyLog Example: Demonstrates how to create a custom broker adapter for Kafka to achieve automatic context propagation.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js"
  },
  "keywords": [
    "syntropylog",
    "kafka",
    "correlation",
    "logging",
    "broker"
  ],
  "author": "Sintropico",
  "license": "Apache-2.0",
  "dependencies": {
    "kafkajs": "^2.2.4",
    "syntropylog": "file:../.."
  },
  "devDependencies": {
    "nock": "^13.5.6",
    "typescript": "^5.2.2"
  }
}
