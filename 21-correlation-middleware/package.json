{
  "name": "21-correlation-middleware",
  "version": "1.0.0",
  "description": "SyntropyLog correlationIdMiddleware (Express) / fastifyCorrelationHook (Fastify)",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "start": "npm run clean && npm run build && node dist/index.js",
    "dev": "npm run clean && tsx src/index.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "syntropylog": "1.0.0-rc.3"
  }
}
