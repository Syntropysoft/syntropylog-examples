# slpy (slpy-log) is installed separately by run.sh — editable from the sibling repo
# for local testing, or `pip install slpy-log` once published. Kept out of this file so
# the local path stays in one place (run.sh).
aiokafka>=0.10
redis>=5.0
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
