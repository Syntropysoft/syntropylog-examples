{
  "name": "31-testing-serializers",
  "version": "1.0.0",
  "description": "Testing patterns for custom serializers with SyntropyLog",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "test": "vitest --run --reporter=verbose",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --run --coverage",
    "build": "tsc"
  },
  "dependencies": {
    "syntropylog": "file:../../.."
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "ts-node": "^10.9.1",
    "vitest": "^1.1.0",
    "@types/node": "^20.10.5"
  }
}
